node_modules/

# Google OAuth credentials — generated locally, injected via env in production
google-credentials.json
google-token.json

# Secrets — never commit
.env

# Logs
*.log

# Personal/internal docs — not for public consumption
implementation_plan.md
PRD.md
PRD-web-platform.md
RUNBOOK.md
TASKS.md

# Curator export files (generated output)
*-export-*.md

# Per-run diagnostic artifacts (scraped events + AI prompt/response snapshots)
runs/
